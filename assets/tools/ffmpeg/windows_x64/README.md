Preferred: use a shared build.
Place `ffmpeg.exe` with its required `*.dll` files in this folder,
or under `bin/` (for example `bin/ffmpeg.exe` + `bin/*.dll`).
UsmDiviner will auto-detect it on Windows x64.
